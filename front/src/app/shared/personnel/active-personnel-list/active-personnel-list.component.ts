import { AfterViewInit, Component, OnInit, ViewChild } from '@angular/core';
import { MatPaginator } from '@angular/material/paginator';
import { MatSort } from '@angular/material/sort';
import { MatTable } from '@angular/material/table';
import { ActivePersonnelListDataSource, ActivePersonnelListItem } from './active-personnel-list-datasource';

@Component({
  selector: 'app-active-personnel-list',
  templateUrl: './active-personnel-list.component.html',
  styleUrls: ['./active-personnel-list.component.scss']
})
export class ActivePersonnelListComponent implements AfterViewInit, OnInit {
  @ViewChild(MatPaginator) paginator: MatPaginator;
  @ViewChild(MatSort) sort: MatSort;
  @ViewChild(MatTable) table: MatTable<ActivePersonnelListItem>;
  dataSource: ActivePersonnelListDataSource;

  /** Columns displayed in the table. Columns IDs can be added, removed, or reordered. */
  displayedColumns = ['id', 'name'];

  ngOnInit() {
    this.dataSource = new ActivePersonnelListDataSource();
  }

  ngAfterViewInit() {
    this.dataSource.sort = this.sort;
    this.dataSource.paginator = this.paginator;
    this.table.dataSource = this.dataSource;
  }
}
